# プロンプト設計

`assets/js/prompts.js` が AI に渡す文字列をすべて組み立てます。UI は AI の出力を JSON として受け取り、構造化して描画します。自由文で返させないのは、選択肢・出典・図を別々の要素として扱うためです。

## システムプロンプト

毎ターン再構築され、次を含みます。

| 部分 | 内容 |
| --- | --- |
| 絶対規則 | 原因を先に言わない／問いは 1 つ／段階的誘導／SunPy 固有事情／捏造禁止／JSON のみ |
| 出題形式 | 選択問題なら choices 4 件必須、記述問題なら空配列 |
| 想定水準 | 入門・実務・上級 |
| 対象パッケージ | `config.js` の `area` と `pitfalls` を展開 |
| 参照ライブラリ | ユーザー登録の論文・ドキュメント（各 2200 字まで） |

## ターンごとの JSON

**Gate A（初手）**

```json
{"opening": "…", "hint": "着眼点のみ 30〜60字"}
```

**Gate B（設問）**

```json
{"phase": "観察", "level": 1, "question": "…", "choices": ["A. …","B. …","C. …","D. …"]}
```

**回答を受けた次のターン**

```json
{"feedback": "40字以内の受け止め", "phase": "…", "level": 2, "question": "…", "choices": [...]}
```

`feedback` で正誤を断定させないのが要点です。断定すると、その時点で探索が止まります。

**判定（「原因が分かった」）**

```json
{"verdict": "correct|close|wrong", "message": "…", "explanation": "correct のみ",
 "sources": [{"title":"","authors":"","year":"","venue":"","url":""}],
 "question": null, "phase": "", "level": 3, "choices": []}
```

`close` / `wrong` のときは `question` が必須です。ここを空で返させると対話が行き止まりになります。

**図の生成（判定とは別呼び出し）**

```json
{"svg": "<svg …></svg>", "caption": "40字以内", "body": "150〜250字"}
```

判定と同じ応答で図まで作らせると `max_tokens` に当たって SVG が途中で切れます。必ず分けてください。SVG は `app.js` の `cleanSVG()` で `<script>` と `on*` 属性を除去してから挿入します。

**Gate C（開示）**

```json
{"cause": "…", "evidence": "…", "fix": "…", "prevention": "…", "sources": [...]}
```

## パースの方針

`api.js` の `grabJSON()` は、最初の `{` から最後の `}` までを切り出して `JSON.parse` します。失敗したら末尾カンマだけ除去して再試行し、それでも駄目なら `null` を返します。UI 側は `null` を「組み立てられませんでした」と表示して再送を促します。Web 検索を有効にすると応答に検索結果ブロックが混ざるため、テキストブロックだけを連結してから切り出しています。

## 調整するときの勘所

- 設問が抽象的すぎるときは `config.js` の `pitfalls` を具体化するのが最も効きます。プロンプト本文より効果が大きいです。
- 選択肢が易しすぎるときは、誤答を「もっともらしいが実際には違う」典型的誤解にする指示を強めます。
- 出典が怪しいときは、参照ライブラリに公式ドキュメントの該当節を貼るのが確実です。捏造禁止だけでは URL の正確さは担保できません。
